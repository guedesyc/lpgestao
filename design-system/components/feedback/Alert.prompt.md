Alert — inline callout banner with a thin colored accent bar (never a full colored left border on a card).

```jsx
<Alert tone="success" title="Contrato assinado">A renovação foi confirmada para o próximo ciclo.</Alert>
```
